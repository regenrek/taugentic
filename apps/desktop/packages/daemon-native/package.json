{
  "name": "@taugentic/desktop-daemon-native",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "default": "./index.js"
    }
  },
  "scripts": {
    "stage-native": "node ./scripts/stage-native.mjs",
    "test": "cargo build --manifest-path ../../../../Cargo.toml --package ta-desktop-native --lib --profile dev && cargo build --manifest-path ../../../../Cargo.toml --package ta-orchestrator --bin ta-daemon --profile release && pnpm run stage-native && bun test ./tests"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
